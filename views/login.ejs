<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <link href="../bs/assets/img/provo2.png" rel="icon">
    <link href="../bs/assets/vendor/bootstrap/css/bootstrap.min.css" rel="stylesheet">
    <link href="../bs/assets/vendor/bootstrap-icons/bootstrap-icons.css" rel="stylesheet">
    <link href="../bs/assets/vendor/aos/aos.css" rel="stylesheet">
    <link href="../bs/assets/vendor/glightbox/css/glightbox.min.css" rel="stylesheet">
    <link href="../bs/assets/vendor/swiper/swiper-bundle.min.css" rel="stylesheet">
    <link href="../bs/assets/vendor/remixicon/remixicon.css" rel="stylesheet">
    <link href="../bs/assets/css/main.css" rel="stylesheet">
    <link rel="stylesheet" type="text/css" href="../bs/assets/css/style.css">
</head>
<body class="page-index">
  <!-- ======= Header ======= -->
  <header id="header" class="header d-flex align-items-center fixed-top">
    <div class="container-fluid container-xl d-flex align-items-center justify-content-between">

      <a href="index.ejs" class="logo d-flex align-items-center">
        <!-- Uncomment the line below if you also wish to use an image logo -->
        <!-- <img src="assets/img/logo.png" alt=""> -->
        <h1 class="d-flex align-items-center">Provo City</h1>
      </a>

      <i class="mobile-nav-toggle mobile-nav-show bi bi-list"></i>
      <i class="mobile-nav-toggle mobile-nav-hide d-none bi bi-x"></i>

      <nav id="navbar" class="navbar">
        <ul>
          <li><a href="index.ejs" class="active">Home</a></li>
          <li><a href="form.ejs">Survey</a></li>
          <li><a href="index.ejs">Dashboard</a></li>
        </ul>
      </nav><!-- .navbar -->

    </div>
  </header><!-- End Header -->

  <form action="action_page.php" method="post">
    <div class="imgcontainer">
        <img src="../bs/assets/img/provoLogo.jpg" alt="Logo" class="logo">
    </div>
  
    <div class="container">
        <label for="username"><b>Username</b></label>
        <input type="text" placeholder="Enter Username" name="username" required>
      
        <label for="password"><b>Password</b></label>
        <input type="password" placeholder="Enter Password" name="password" required>
      
        <a href="">
          <button type="submit" onclick="login();">Login</button>
        </a>
    </div>
  
    <div class="container">
      <a href="createAccount.ejs">
        <button type="button" class="createbtn" onclick="">Create Account</button>
      </a>
        <span class="password"><a href="modifyAccount.ejs">Forgot password?</a></span>
    </div>
  </form>

  <script>
    const adminUsername = 'admin';
    const adminPassword = 'admin123';

    function login()
    {
      const username = document.getElementById("username").value;
      const password = document.getElementById("password").value;

      if (username === adminUsername && password === adminPassword)
      {
        window.open('otherlksd.html');
      }
      else
      {
        alert("Invalid username or password")
      }

      if (!localStorage.getItem(username))
      {
        alert("You don't have an account.");
      }
      else
      {
        const storedPassword = localStorage.getItem(username);

        if(storedPassword && storedPassword === password)
        {
          alert("Login successful");
          window.open('lskd.html');
        } 
        else
        {
          alert("Invalid username or password");
        }
      }

      document.getElementById("username").value = "";
      document.getElementById("password").value = "";
    }
  </script>
</body>
  <!-- Template Main JS File -->
  <script src="../bs/assets/js/main.js"></script>
</html>