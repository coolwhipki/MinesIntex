<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Records</title>
</head>
<body>
    <label><b>Country Information:</b></label>
   <br><br>
   <table style="width: 100%; background-color: antiquewhite; border: 1px solid black;">
     <thead style="background-color: cornflowerblue; color: white;">
        <!-- CHANGE THESE TO TITLES AND SUCH -->
       <tr>
         <th style="text-align: left;">Name</th>
         <th style="text-align: left;">Popular Site</th>
         <th style="text-align: left;">Capital</th>
         <th style="text-align: left;">Population</th>
         <th style="text-align: left;">Visited</th>
         <th style="text-align: left;">Covid Level</th>
         <th></th>
         <th></th>
       </tr>
     </thead>
     <tbody style="font-family: Arial; font-size: 9pt;">
      <% for (let iCount = 0; iCount < adminInfo.length; iCount++) { %>
        <!-- now we are writing html so we don't need the ejs tag (javascript structure code) guys -->
         <tr>
            <!-- less than percent equal sign means you are pulling data; the column names need to match exactly-->
            <!-- CHANGE ALL OF THESE GUYS TO MATCH OUR COLUMNS and such -->
           <td><%= userInfo[iCount].country_name %></td>
           <td><%= userInfo[iCount].popular_site %></td>
           <td><%= userInfo[iCount].capital %></td>
           <td><%= userInfo[iCount].population %></td>
           <td style="text-align: center;"><%= adminInfo[iCount].visited %></td>
           <td style="text-align: center;"><%= adminInfo[iCount].covid_level %></td>
           <td style="text-align: center;">
            <form action="/addRecord/<%= mycountry[iCount].country_id %>" method="GET">
                <button type="submit">Edit</button>
              </form>
            </td>
</body>
</html>